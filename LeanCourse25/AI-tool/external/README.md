# External Tool Links

This folder holds local links to external tool repos that are used by `AI-tool`.

Current intended links:

- `OpenGauss`

These are recreated from `config\profiles.local.json` via:

```powershell
powershell -ExecutionPolicy Bypass -File .\LeanCourse25\AI-tool\scripts\Sync-AIToolLinks.ps1
```
